'use babel';

import Apm install fileIconsView from '../lib/apm install file-icons-view';

describe('Apm install fileIconsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
